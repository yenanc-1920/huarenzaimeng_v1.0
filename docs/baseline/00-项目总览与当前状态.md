---
文档状态: 维护中
文档负责人: 空空（PM／总调度）
适用阶段: V1 当前交付
最后更新: 2026-08-21
是否为执行基线: 是
---

# 项目总览与当前状态

## 项目目标

交付“华人在孟”微信小程序及运营后台，覆盖充值、订单、身份、内容与运营管理，并以失败关闭、证据分级和环境隔离为基本原则。

## 当前 Git 与发布状态

- 固定工作目录：`E:\workspace\huarenzaimeng`
- 开发分支：`codex/v1-delivery-recovery`
- DEV 集成分支：`dev`
- DEV 自动部署分支：`deploy/dev`
- 当前已推送提交：`4a9d2e02db06899634202bb0d141c19b144d15f7`
- `dev` 与 `deploy/dev` 已同步到上述提交。
- 已确认线上版本：`huaren-api-dev-028`，创建时间 `2026-08-21 14:44:50`，对应上一业务提交 `c0f8d11`。
- CI 缓存优化提交 `4a9d2e0` 的 GitHub 门禁和部署分支晋级已成功；其 CloudBase 精确实例版本仍需下一次控制台证据确认。
- 工作区存在接任前文档改动，禁止清理或整体提交；精确清单见 Git 状态和 `11-来源与历史映射.md`。

## 当前阶段

V1 DEV 真实集成收口阶段。代码候选、本地合同和构建已形成；真实微信登录链正在做部署后验证。真实支付、真实 WINLA、TEST/STAGE/PROD 晋级、视觉签核和正式小程序发布均未完成。

## 当前唯一首因

`OPEN-01`：CloudBase DEV 的微信 `code2Session` HTTPS 被平台 SafeLink 自签名证书截获。已实现严格 DEV-only 官方云调用 HTTP 通道，固定环境、host、path 和 `x-openapi-rule=bypass`，未关闭 TLS 校验、未导入动态自签 CA、未扩大到其他接口。

当前代码与定向验证已完成并部署到 DEV 发布链；关闭前仍需使用全新一次性 `wx.login` code 做一次受控真实登录验证，且不得记录 code、secret、session_key、完整 query 或原始响应。

## 已完成

- V1 产品、UI、架构、开发骨架及主要业务合同已建立。
- Flyway 候选迁移演进至 V24；DEV 数据库历史已执行至 V24。
- 微信身份边界、报价/订单快照、支付与充值协调内核、UNKNOWN 恢复、outbox、后台 A100/A110/A140 等已实现。
- 小程序 16 个业务页面和后台 21 个 frame 具备静态追踪与构建合同。
- OPEN-01 SafeLink 有界实现通过安全复核、21 项合并后定向测试和发布门禁。
- DEV 镜像构建已增加 Buildx＋GHA 跨 Run 缓存；首次冷缓存成功导出，下一次自然运行验证热缓存收益。

## 未完成与外部依赖

- `OPEN-01`：真实 `wx.login` 登录闭环。
- `OPEN-02`：Flyway/MySQL 偶发 reset 观察。
- `OPEN-03`：微信支付真实小额联调，保持 disabled。
- `OPEN-04`：WINLA 真实合同与小额联调，保持 disabled。
- `OPEN-05`：MySQL 5.7 专用受控验证。
- `OPEN-06`：TEST/STAGE/PROD 同提交晋级。
- `OPEN-07`：法律文本终审。
- `OPEN-08`：真实截图与视觉签核。
- `OPEN-09`：小程序正式上传、审核和发布。
- 外部依赖包括微信/CloudBase、WINLA、真实数据库授权、法律终审和南哥的资金/生产确认。

## 风险边界

- 本地、Fake、H2、静态和构建结果不得冒充真实集成。
- 真实资金、生产发布、密钥变更、数据库破坏性操作和需求取舍必须暂停请南哥确认。
- 其他在批准范围内的可逆开发、定向测试、功能分支提交和推送可自主执行。

