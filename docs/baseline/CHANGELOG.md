# 基线变更记录

## 2026-08-24 — V2-H5-IMPLEMENT-01 本地候选与云托管制品接入

- 多角色评审确认采用最小复用方案：保留V1源码、后端和数据结构，复用现有uni-app首页、双时钟、资讯、黄页、公共样式与公共读API；通过V2路由、导入和网络闭包隔离身份与交易能力，不重写既有系统。
- V2用户端收敛为首页／资讯／黄页三Tab及法律合作页，使用独立公共H5客户端；唯一用户点击外链精确固定到WINLA推广页，并明确第三方承担充值、订单、退款与售后。
- 五套环境Dockerfile将H5制品挂载到现有Spring应用`/h5/`，Vite静态资源基路径同步固定为`/h5/`；后台根路径与API路径不迁移。
- V1的18路由冻结为独立归档清单，旧视觉追溯继续校验旧源码；DEV发布门禁不再构建V1小程序制品，改为强制执行V2 H5源码与产物闭包。
- 本地源码合同29项、H5产物门禁18项、TypeScript、H5构建和完整DEV门禁通过；后端全量938项失败0、跳过65。新增`/h5`与`/h5/`固定入口映射及MVC测试。本机不存在Docker，真实镜像构建、浏览器视觉、微信云托管DEV和公众号内真机仍未执行，不得登记为DEV_PASS。
- 固定提交`d9fa94cd81d04efead296b7d50b5d6d2ff9c1fa3`已推送V2分支并快进合并`dev`；GitHub Run `32660141791`于2026-08-24 01:05:41至01:13:35（孟加拉时间）成功完成完整门禁和真实DEV镜像构建，`origin/deploy/dev`推进到同提交。该证据不等于微信云托管实例启动、页面加载或业务验收。

## 2026-08-24 — V2-H5-MAJOR-01 重大版本转向

- 南哥批准V1.0微信小程序版冻结归档，V2.0公众号入口H5版成为当前唯一开发目标；对外名称仍为“华人在孟”。
- V2固定为首页／双时钟、资讯、黄页、法律与合作说明及一个WINLA推广入口；取消普通用户登录、手机号、运营商识别和我方全部充值、订单、支付、退款及充值售后。
- 唯一充值卡片直接跳转WINLA固定推广页，页面明示小啦全球充承担充值、订单、退款和售后；我方不收佣金、不经手资金、不保存或接收第三方订单。
- 新增15至18号V2产品、UI、技术和计划验收基线；03至05、12至14号冻结为V1归档，README、00、01、08、09、10同步版本路由和执行状态。
- 当前本地HEAD／`origin/dev`为`df47552f`，`origin/deploy/dev`为`7e476dd`，二者不一致并分别登记；未创建远程归档标签／分支，未发布、未改公众号、域名、证书、密钥或数据库。

## 2026-08-23 — 运营商Logo与交互文字垂直居中

- P010识别结果移除“号码”文字占位图，按五个固定`operatorCode`展示对应运营商Logo图片，运营商判定合同不变。
- 小程序公共按钮、顶部返回、分类/筛选Tab、列表项、底部导航和弹层操作统一补充垂直居中规则；普通多行正文保持原排版。
- 当前仅为本地候选；须经前端合同、DEV构建、AppService加载以及微信开发者工具或真机视觉复核后关闭。

## 2026-08-23 — 微信小程序代码质量扫描修复

- 首次将两张超大静态图优化为256×256后，媒体资源合计仍超过扫描的200KB口径；现纠正为192×192，`logo.png`约84KB、`default-avatar.png`约50KB，媒体合计133993字节。
- 微信构建开启JS压缩标志和`requiredComponents`组件按需注入，并在构建验证脚本中增加生成配置断言。
- 构建门禁同步改为检查图片与音频资源合计不超过200KB；DEV构建、AppService加载和环境身份检查通过。微信开发者工具代码质量重新扫描仍为`NOT_RUN`，不得由本地构建替代。

## 2026-08-23 — 注销终态规则与四项小程序体验调整

- 南哥批准注销规则：申请后立即退出；无未结事项时原则上24小时内完成；有未结事项时处理完成后继续清理；最终注销后同一微信可注册为全新账号，但不得恢复或关联旧账号数据。
- release注销清理调度改为默认开启；最终关闭时以不可逆墓碑摘要释放旧微信身份唯一键，保留依法需保留且与新账号隔离的交易记录。
- P001刷新按钮与P042分类按钮补充双轴居中；用户协议/隐私政策返回区域固定；P024无头像时改用生成的无文字默认熊猫图案。当前均为本地候选，须经定向测试、构建和真机视觉复核。
- 后端生命周期、清理worker、调度装配和认证控制器定向测试16/16通过；小程序前端合同、正式运行合同、DEV构建及AppService加载通过，默认头像已进入构建产物。
- 当前证据仅为`LOCAL_PASS`：注销终态和重新注册须在固定DEV版本验证；四项样式须在微信开发者工具或真机人工签核，构建成功不等于视觉通过。

## 2026-08-23 — FN-MP-04 注销申请DEV验收关闭

- 固定提交`7e476dd`通过GitHub Run `32622346294`并推进`deploy/dev`；南哥使用已授权专用账号`Kongkong_1920`确认可以提交注销，页面提示账号清理中。
- 同一账号再次登录提示注销申请处理中且无法登录，证明申请受理、活跃会话失效及登录阻断符合合同；`FN-MP-04`与P024相关状态登记为`DEV_PASS`。
- 证据边界：本次不证明PII清理任务已最终完成或账号已进入`CLOSED`，不得重复提交注销或把登录阻断外推为最终清理完成。

## 2026-08-23 — FN-MP-04 DEV失败定位与V26本地修复

- 南哥授权专用微信测试账号`Kongkong_1920`执行注销；确认框正常，但微信云托管`huaren-api-dev-051`在2026-08-23 13:32:41（控制台北京时间）报`Data too long for column 'status_code'`，事务回滚，账号未注销。
- 根因为`buyer_identity.status_code VARCHAR(16)`无法容纳17字符的`CLOSURE_REQUESTED`；同一事务随后写入的`buyer_consent_state.consent_state`也为16字符容量。
- 新增V26，仅将上述两个字段扩至`VARCHAR(24)`；生命周期、控制器与迁移合同定向测试14/14通过。当前仅`LOCAL_PASS`，未推送、未部署、未执行DEV数据库迁移，唯一下一动作是发布后单次重测。
- 首次推送提交`cca3e7b`触发GitHub Run `32620936447`，因两处迁移清单仍固定到V25而失败关闭，未推进`deploy/dev`、未触发微信云托管部署或DEV数据库迁移。
- 已同步V26终态迁移计数和清单断言；定向测试24/24、与GitHub相同的本地完整DEV门禁通过（后端937项、失败0、跳过65，最终`DEV_DEPLOYMENT_GATE_GO`）。须取得新的明确推送授权后再生成新Run，不重跑失败Run。

## 2026-08-23 — FN-MP-04 注销申请就绪门禁

- 注销生命周期与认证控制器定向测试13/13通过；申请会立即撤销全部会话、把账号置为`CLOSURE_REQUESTED`并创建PII清理任务，未结交易只阻塞最终关闭，不阻止先禁止重新登录。
- DEV真实执行保持`NOT_RUN／NEEDS_TEST_ACCOUNT_AUTHORIZATION`；必须由南哥指定并授权可永久进入注销流程的专用微信测试账号，不得使用当前订单验收账号。

## 2026-08-23 — FN-MP-03／P020 定向DEV验收关闭

- 固定提交`caa783f`通过GitHub DEV门禁并推进`deploy/dev`；南哥确认登录后进入“我的订单”不再提示登录，并确认从P024“我的”和P020“我的订单”触发登录后均返回原页面。
- 结合固定提交`ef7f6db`已确认的退出与重新登录结果，FN-MP-03登记为`DEV_PASS`；P020会话门禁回归关闭，FN-MP-10恢复`DEV_PASS`。
- 证据边界为南哥在微信开发者工具中的人工结果反馈；本轮未独立绑定精确微信云托管实例编号，未触发注销、支付、WINLA、数据库或生产。

## 2026-08-23 — FN-MP-03 登录返回与P020会话门禁

- 南哥确认固定提交`ef7f6db`在微信开发者工具中可以退出并重新登录，`FN-MP-03-LOGOUT-01`登记为`DEV_PASS`；未触发注销。
- 新发现P020仍用旧`projectSessionProjection`判断登录，导致真实BUYER token存在时仍提示登录；现改为BUYER token唯一门禁，API继续携带Bearer读取。
- 登录成功不再固定跳首页，仅允许返回P024“我的”或P020“我的订单”两个固定来源；前端合同、正式运行合同及DEV构建通过，尚未推送和发布，状态为`LOCAL_PASS／DEV_RETEST_REQUIRED`。

## 2026-08-23 — FN-MP-03 退出合同与P024身份卡

- RunId `FN-MP-03-DEV-20260823-01`确认个人中心登录态和退出确认框通过；退出请求在微信云托管返回400／`LOGOUT_REQUEST_INVALID`，确认不是开发者工具缓存，本次停止后未重试登录。
- 后端退出合同仅兼容无请求体或空JSON对象，仍拒绝query、非对象和带字段对象；定向测试9/9通过。P024登录态身份卡按南哥决定只显示头像和用户名，不再显示默认头像或头像昵称授权说明。
- 前端合同、正式运行合同和微信小程序DEV构建通过；状态仅为`LOCAL_PASS／DEV_RETEST_REQUIRED`，未发布微信云托管，未触发注销、支付、WINLA、数据库或生产。

## 2026-08-23 — FN-MP-07／FN-MP-10 既有验收状态纠偏

- 南哥确认P010、P020、P021、P014均已在原任务完成验证；撤销基线中的重复DEV复测安排，四项按既有验收登记为`DEV_PASS`。
- 证据边界：当前已定位旧归档任务，但尚未从归档正文恢复固定微信云托管实例、观测时点和截图索引，因此附记`ARCHIVED_ACCEPTANCE_INDEX_PENDING`，不虚构缺失元数据，也不重跑四项。
- P014只覆盖无资金订单进度读取，P021只覆盖订单详情读取；真实支付、WINLA、退款和P023历史订单恢复仍按各自验收项执行。

## 2026-08-22 — OPEN-11-ORDER-READ-01／DETAIL-01／PROGRESS-01

- P020、P021、P014统一消费release BUYER只读接口的`ProjectEnvelope`与`OrderProjection`；列表不再发送旧授权查询参数，详情不再把客户端`authorizedOrderRefs`当订单所有权门禁。
- P014删除支付、充值提交和原单查询写入口，只保留服务端订单投影刷新；支付与WINLA disabled期间不会从该页触发写动作。
- release合同定向测试、TypeScript检查及DEV小程序构建通过；状态仅为`LOCAL_PASS／DEV_RETEST_REQUIRED`，P023恢复、匿名认领/迁移/合并及任何资金、部署、数据库动作均不在本次范围。

## 2026-08-22 — FN-MP-07-00880-01

- 资格接口删除独立号码正则，改为复用报价入口已有的孟加拉号码归一化；共享入口仅补齐获批的`00880`国际拨号前缀，不改变运营商映射或资格业务。
- 先以SYN零号段回归用例复现HTTP 400，再完成资格接口与共享归一化定向测试13/13通过；状态仅更新为`LOCAL_PASS／DEV_RETEST_REQUIRED`，不外推`DEV_PASS`。
- 未调用外部接口，未改报价、订单、支付、WINLA、数据库或部署；唯一下一动作是发布后只复测三种获批号码格式并补页面证据。

## 2026-08-22 — FN-MP-07-DEV-01

- RunId `FN-MP-07-DEV-20260822-01` 仅使用仓库中明确标识为 SYN 的零号段夹具，对 `dev.guiye.xyz` 执行3次无凭据只读资格请求；未保存完整 query、响应体或手机号。
- 本地 `01…`与国际 `8801…` 格式均 HTTP 200，运营商识别为 GRAMEENPHONE且掩码形态通过；基线明确接受的 `008801…` 格式返回 HTTP 400／`RECHARGE_INPUT_INVALID`，形成有界 DEV 接口失败证据。
- 前两项资格结果均为 `UNKNOWN`；未执行页面验证，也未独立绑定精确微信云托管实例，因此 `FN-MP-07` 不得登记整体 `DEV_PASS`；未调用报价、订单、支付或 WINLA。

## 2026-08-22 — FN-MP-06-DEV-01

- 2026-08-22T17:21:13Z至17:22:01Z仅对`dev.guiye.xyz`执行无凭据只读GET：生活资讯列表HTTP 200且返回2项，首条详情HTTP 200并与列表引用一致，`publishedAt`、`updatedAt`、`validUntil`存在；明显不存在的内容标识返回HTTP 404。
- 本轮没有可用的微信云托管控制面只读能力，不能独立绑定观测时点的精确实例；API子项登记为`DEV_PASS`，FN-MP-06整体仅为`PARTIAL_DEV_PASS`，不得据基线中的DEV049／`628385f`外推完整版本身份。
- P042/P043页面、列表到详情导航、时间展示与页面异常态因无可审计小程序浏览器保持`NOT_RUN`。未登录、未写入数据，未触发订单、报价、支付、WINLA、退款、发布、环境、密钥或数据库操作。

## 2026-08-22 — FN-MP-05-DEV-01

- 2026-08-22T17:20:57Z仅对`dev.guiye.xyz`执行4次无凭据只读GET：城市、DHAKA列表、LIFE_SERVICE筛选和同一条目详情均HTTP 200；列表与筛选计数、分类匹配、详情引用和必填字段均符合合同。
- 本轮没有可用的微信云托管控制面只读能力，不能独立绑定观测时点的精确实例；证据仅登记为`PARTIAL_CONTROLLED_RUNTIME`，不得据基线中的DEV049／`628385f`整体升级为`DEV_PASS`。
- P040/P041页面、空态、异常态因无可审计小程序浏览器保持`NOT_RUN`；举报POST会写入真实DEV，未获单次写入授权，保持`NOT_RUN`。未登录、未创建数据、未触发交易、资金、发布、环境、密钥或数据库操作。

## 2026-08-22 — CLOUD-PLATFORM-IDENTITY-01

- 冻结平台身份：本项目服务器运行与部署平台为“微信云托管”，禁止称为或推断为“腾讯云服务器／腾讯云部署”。
- `CloudBase` 仅在微信云托管控制面、现有工具或证据原文语境中保留；服务版本、实例、控制台状态和日志时间均按微信云托管证据记录。
- 调整DEV环境与发布链表述并增加防复发约束；未机械替换技术标识，未改写历史证据文件。

## 2026-08-22 — HANDOVER-AGENT-COMMS-01

- 新增智能体委派与消息收口规则：空空主会话只保留问题摘要和终态，大量读取、执行、测试日志及中间排查由独立智能体承载。
- 智能体默认自行完成，仅在终态、确需授权或固定对象漂移时回传；禁止逐步转发普通进度、完整日志、长回执或大量截图。
- 智能体不改变既有角色职责、不绕过权限或代替专业评审；结论仍须归档对应基线，外部验证和南哥批准边界保持不变。

## 2026-08-22 — HANDOVER-TEMPLATE-01

- 在角色通讯基线中统一角色终态交接模板，收敛任务 ID、状态、P0/P1、固定对象、验证计数、执行边界、证据等级、残留状态和唯一下一动作。
- 不适用字段必须显式填写，`NOT_RUN` 不得冒充 `PASS`；部署、外部调用、真实资金、生产、秘密和破坏性数据库操作继续保留原授权边界。
- 本次只修改唯一对应基线正文与变更记录，不回写历史交接文件，不重置角色或扩大权限。

## 2026-08-22 — OPEN-11-BE-06 DEV 049小程序定向验收通过

- 固定提交`628385f`首次发布DEV 048时在镜像仓库推送阶段因`blob unknown to registry`失败，未创建实例；唯一一次有界重发DEV 049镜像推送、实例启动与健康探针通过，服务正常且1个实例。
- 绑定域名验证健康与公开目录HTTP 200，公开目录与Quote的`catalogVersion`均为2026082201；新匿名会话HTTP 201、报价HTTP 200且Quote存在。
- 小程序返回商品页重新选择后，订单确认页正常展示报价摘要和倒计时，确认订单按钮可用；`OPEN-11-BE-06`与`FN-MP-09`据此更新为`DEV_PASS`。未重编译小程序，未把部署健康或接口探针单独冒充小程序验收。

## 2026-08-22 — OPEN-11-BE-06 DEV 047目录版本错源

- DEV 047接口级报价与建单通过，但小程序在报价HTTP 200后本地失败关闭；公开目录返回支持集/目录版本1，而Quote返回真实权威目录版本2026082201。
- 根因是公开目录把商品`aggregate_version`同时作为支持集与目录版本；本次只改为选择当前有效最大支持集及其下最大有效目录，并补充与报价版本一致性测试。
- 后端相关定向测试18/18、小程序正式运行时与发布环境合同、统一基线检查均通过；重新编译小程序不能修复该后端版本错源，本地证据不冒充小程序DEV通过，发布后必须返回商品页重新选择再验收。

## 2026-08-22 — OPEN-11-BE-06 DEV 046可空价格来源

- DEV 046已越过支持集缺失导致的409；匿名会话HTTP 201后报价HTTP 500，北京时间14:42:06日志确认价格快照使用`Map.of`接收可空`supplierSourceRef`并触发`NullPointerException`。
- 本次仅复用既有`nullable()`规则序列化该字段并增加NULL回归测试；不改seed、表结构、支付或WINLA。
- 报价服务与DEV数据合同定向测试16/16、统一基线检查通过；固定提交`6d5037a`发布为微信云托管`huaren-api-dev-047`后，绑定域名健康200、匿名会话201、报价200且Quote存在、同主体建单200且Order存在，OPEN-11-BE-06关闭。

## 2026-08-22 — OPEN-11-BE-06 DEV 045支持集缺口

- DEV 045中匿名会话创建HTTP 201，报价仍HTTP 409；真实DEV只读SQL确认启用渠道与当前有效价格均唯一，但当前支持集/目录为空且运营商成员关系为0，故报价权威查询得到零行。
- DEV repeatable样例补齐固定的当前有效支持批次、五运营商`SUPPORTED`成员关系和对应有效目录；不覆盖历史/人工版本，不改表结构，也不放宽报价、支付或WINLA门禁。
- 本地报价服务与DEV数据合同定向测试15/15通过，统一基线检查通过，且`local-devdata`制品包含目标seed；上述证据不冒充DEV通过，发布固定候选后只重跑一次匿名报价与建单。

## 2026-08-22 — OPEN-11-BE-06 DEV 044根因纠正

- DEV 044已包含价格稳定选择，但`TRUSTED_PRODUCT_PRICE_NOT_UNIQUE`也用于报价查询零行，不能据错误码继续推断为重叠价格多行。
- DEV repeatable样例商品此前被公开目录返回，却未设置报价门禁要求的`MAPPED`与`AVAILABLE`；本次只补齐样例数据权威状态，不放宽报价、支付或WINLA门禁。
- 本地定向验证不冒充DEV通过；唯一下一动作仍是发布固定候选并重跑匿名报价与建单。

## 2026-08-22 — OPEN-11-BE-06

- DEV 042 已证明匿名会话创建成功，随后报价因公开目录与报价服务对重叠有效价格采用不同选择规则而返回 `TRUSTED_PRODUCT_PRICE_NOT_UNIQUE`。
- 报价服务改为复用公开目录的稳定价格选择：优先 `effective_from` 最大，时间相同时以 `price_version_ref` 最大者裁决；订单仍重验 Quote 冻结的精确价格版本。
- 本地定向验证不冒充 DEV 报价通过；唯一下一动作是发布固定候选并重跑匿名报价与建单，不启用支付或 WINLA。

## 2026-08-22 — OPEN-11-ARCH-02

- 纠偏提交`634bdccc`对应的“报价前必须具有BUYER会话”方向，冻结游客无资金主链为服务端签发匿名会话→报价→建单。
- 匿名会话绝对有效期24小时且不续期，使用服务端高熵token并仅保存摘要；`guestRef`只关联、不认证。
- 匿名Bearer仅允许`POST /buyer-api/v1/quotes`和`POST /buyer-api/v1/orders`；个人订单、账户、退出和注销仍只允许BUYER。
- V1不做匿名订单认领、迁移或主体合并；本次不启用或放宽真实微信支付、WINLA及资金事实。
- 当前基线已冻结但实现仍为`NO-GO`，唯一下一动作是完成匿名会话及Quote/Order双主体的有界实现和定向验证。

## 2026-08-21 — OPEN-11-FE-03

- 固定HEAD重建后的DEV证据排除旧产物；脱敏夹具确认新鲜GP选择通过目录稳定身份校验、命令身份可创建，随后因缺少有效BUYER会话在报价网络调用前同步抛出`BUYER_SESSION_REQUIRED`。
- P012不再把该身份门禁误报为“商品信息已更新”，而是保留选择并进入既有微信快捷登录；目录、商品、面额、类型和价格版本守卫保持不变。
- 本地结果不冒充DEV报价成功；唯一下一步是在有效BUYER会话下从P011重新选择并确认实际发出报价请求。

## 2026-08-21 — OPEN-11-FE-02

- DEV039确认唯一目录校验仍把名称、权益说明和有效期展示文案作为商品身份，导致同一商品及价格版本在报价POST前被本地阻断。
- 客户端改为严格保留运营商、支持集/目录版本、商品、面额、类型和价格版本校验；展示文案差异不再单独否决报价。
- 新增GP-BALANCE-100同形正例及版本、商品、面额、价格版本漂移负例；不涉及后端、支付、WINLA、数据库或部署。

## 2026-08-21 — OPEN-11-FE-01

- 删除P012页面进入时与`api.createQuote`紧邻的重复目录复拉校验，保留API客户端在报价POST前的唯一校验及后端最终权威校验。
- 新增定向合同，锁定新鲜选择只经过一次目录复拉后发出报价请求，真实目录或商品失效仍在POST前失败关闭。
- 同步更新技术约束、问题台账和V1验收矩阵；不涉及支付、WINLA、后端、数据库或部署。

## 2026-08-21 — OPEN-12-DEV-038-01

- 将当前提交与已确认 CloudBase DEV 版本更新为 `2ff264c` / `huaren-api-dev-038`，登记精确实例与发布链。
- 依据固定 038 实例首次启动日志和业务探针证据关闭 OPEN-12。
- 将一次未影响探针的 Hikari `connection closed` 告警归入 OPEN-02／P1 继续观察，不触发重启或配置修改。

## 2026-08-21 — OPEN-12-FLYWAY-SCHEMA-01

- DEV 037 确认数据库连接成功，但自定义 Flyway 在执行迁移 SQL 前因 `currentSchema=null` 启动失败；DEV 已回退 036。
- `releaseFlyway` 改为复用已冻结的 `hz.environment.database-name` 作为 `defaultSchema`，不新增平台环境变量、不 repair、不改迁移 SQL。

## 2026-08-21 — OPEN-11-QUOTE-CATALOG-02

- DEV复测确认首轮修复仍会跨多个有效运营商支持集连接商品；报价选择现与公开目录完全对齐为“最高有效支持集＋其中最高有效目录”。
- 新增双有效支持集回归测试；报价服务定向测试8/8通过，真实DEV验证待本次发布后执行。
- 不改变目录内容、价格、支付、充值提交或数据库结构。

## 2026-08-21 — OPEN-11-QUOTE-CATALOG-01

- P012 报价创建统一绑定同一运营商支持集下版本号最大的当前有效目录，避免历史目录仍有效时把单个商品误判为多条可信商品。
- 新增双有效目录回归测试；报价服务定向测试7/7通过，真实DEV验证待发布后执行。
- 同步更新技术约束、问题经验和V1验收矩阵；不涉及支付、充值提交、真实资金或数据库结构变更。

## 2026-08-21 — P010-PHONE-LOCAL-01

- P010 手机号输入改为孟加拉用户熟悉的 `01xxxxxxxxx` 11位本地格式，不再拆分显示固定 `+880`。
- 同步更新产品需求、UI/高保真交互口径、需求—实现映射和前端实现；服务端继续统一号码规范化。
- 本次不改变运营商映射、商品目录、报价、订单、支付或充值提交规则。

## 2026-08-21 — OPEN-01 CLOSED

- 固定DEV实例真实微信登录全链通过：会话创建201、会话确认200且为`BUYER_SESSION_READY`，页面进入已登录状态。
- `FN-MP-02`更新为`REAL_PASS`，项目转入TRIAL-02无资金DEV业务旅程。
- 登记`SEC-DEV-01`：验证截图再次展开临时令牌；仓库与台账不保存令牌，后续证据必须折叠敏感响应。

## 2026-08-21 — OPEN-01/OPEN-10

- 固定DEV实例真实微信会话创建返回201，确认SafeLink、微信身份和TLS首因已闭合。
- 发现登录后会话确认误请求不存在的 `/buyer-api/v1/session`；新增OPEN-10并改为真实存在的 `/buyer-auth/v1/session`，验证稳定业务字段后进入已登录状态。
- 登记DEV证据截图误展开会话令牌事件；文档不保存令牌，后续截图禁止展开敏感响应。

## 2026-08-21 — PLAN-MAP-01

- 新增 `14-V1需求UI实现映射.md`，逐项映射16个需求业务页面、2个法律路由、42个SVG、后台工作区和28项验收矩阵。
- 收敛统计口径：18个正式路由=16个业务页+2个法律页；后台21个正常态SVG不含状态组件板和早期A130综合设计源。
- `TRIAL-01`更新为来源和映射已冻结；视觉一致性及逐状态真实数据证据仍待执行。

## 2026-08-21 — PLAN-V1-SOURCE-01

- 将 `项目管理/V1需求规格/` 确认为V1需求与UI的首要历史来源，并在来源映射中明确各文件职责。
- 新增 `13-V1上线试运行执行规划.md`，按全功能跑通、真实外部证据、候选晋级和小范围生产试运行重排11个工作包。
- 明确原始需求、后续批准决策、当前实现和运行证据的裁决顺序，禁止用现有代码反向删除需求。
- 明确完整门禁按批次候选执行，小问题只做定向验证，避免每次小改触发重型发布。

## 2026-08-21 — TRIAL-01

- 新增 `12-V1全功能验收矩阵.md`，以28个唯一功能 ID 覆盖小程序、运营后台及跨域上线能力。
- 以代码中的18个小程序页面、8个后台工作区和正式 API 入口校正验收范围。
- 建立时不追认未绑定固定版本的历史结果；DEV_PASS/REAL_PASS 计数从0开始，避免静态或本地证据冒充真实跑通。
- 当前唯一首个阻断项仍为 `FN-MP-02 / OPEN-01`。
- 校正小程序口径为18个已登记正式页面；`pages/support/case.vue` 为未登记、无正式导航且后端入口缺失的非执行残留，登记为 `GAP-UI-01`，不擅自接入V1。

## 2026-08-21 — PLAN-TRIAL-001

- 当前交付目标调整为“全部 V1 功能跑通并启动上线试运行”。
- 任务顺序改为：OPEN-01 清零→DEV 全功能→真实小额交易→TEST/STAGE 同提交晋级→试运行准备→PROD 小范围试运行。
- 增加 TRIAL-01 至 TRIAL-06 唯一任务编号、责任角色、状态、证据路径、下一动作和完成条件。
- 明确阶段内定向修复、阶段末集中候选发布，避免每个小问题触发重型发布。
- 真实资金和 PROD 发布仍须南哥对冻结版本、金额、环境和 RunId 分别授权。

## 2026-08-21 — BASELINE-001

- 建立 `docs/baseline/` 唯一项目基线目录。
- 归拢项目总览、治理权限、角色通讯、产品需求、UI、技术架构、开发约束、质量证据、部署运维、问题经验和版本计划。
- 根目录增加 `AGENTS.md`，强制所有角色与任务从本基线读取当前要求。
- 增加 `tools/check-project-baseline.mjs` 轻量结构核验器，不触发构建或部署。
- 旧文档统一降级为来源、历史、批准记录或证据，不删除、不覆盖。
- 登记接任前现有未提交文档，禁止误清理或误提交。
