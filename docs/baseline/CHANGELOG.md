# 基线变更记录

## 2026-08-22 — OPEN-11-BE-06

- DEV 042 已证明匿名会话创建成功，随后报价因公开目录与报价服务对重叠有效价格采用不同选择规则而返回 `TRUSTED_PRODUCT_PRICE_NOT_UNIQUE`。
- 报价服务改为复用公开目录的稳定价格选择：优先 `effective_from` 最大，时间相同时以 `price_version_ref` 最大者裁决；订单仍重验 Quote 冻结的精确价格版本。
- 本地定向验证不冒充 DEV 报价通过；唯一下一动作是发布固定候选并重跑匿名报价与建单，不启用支付或 WINLA。

## 2026-08-22 — OPEN-11-ARCH-02

- 纠偏提交`634bdccc`对应的“报价前必须具有BUYER会话”方向，冻结游客无资金主链为服务端签发匿名会话→报价→建单。
- 匿名会话绝对有效期24小时且不续期，使用服务端高熵token并仅保存摘要；`guestRef`只关联、不认证。
- 匿名Bearer仅允许`POST /buyer-api/v1/quotes`和`POST /buyer-api/v1/orders`；个人订单、账户、退出和注销仍只允许BUYER。
- V1不做匿名订单认领、迁移或主体合并；本次不启用或放宽真实微信支付、WINLA及资金事实。
- 当前基线已冻结但实现仍为`NO-GO`，唯一下一动作是完成匿名会话及Quote/Order双主体的有界实现和定向验证。

## 2026-08-21 — OPEN-11-FE-03

- 固定HEAD重建后的DEV证据排除旧产物；脱敏夹具确认新鲜GP选择通过目录稳定身份校验、命令身份可创建，随后因缺少有效BUYER会话在报价网络调用前同步抛出`BUYER_SESSION_REQUIRED`。
- P012不再把该身份门禁误报为“商品信息已更新”，而是保留选择并进入既有微信快捷登录；目录、商品、面额、类型和价格版本守卫保持不变。
- 本地结果不冒充DEV报价成功；唯一下一步是在有效BUYER会话下从P011重新选择并确认实际发出报价请求。

## 2026-08-21 — OPEN-11-FE-02

- DEV039确认唯一目录校验仍把名称、权益说明和有效期展示文案作为商品身份，导致同一商品及价格版本在报价POST前被本地阻断。
- 客户端改为严格保留运营商、支持集/目录版本、商品、面额、类型和价格版本校验；展示文案差异不再单独否决报价。
- 新增GP-BALANCE-100同形正例及版本、商品、面额、价格版本漂移负例；不涉及后端、支付、WINLA、数据库或部署。

## 2026-08-21 — OPEN-11-FE-01

- 删除P012页面进入时与`api.createQuote`紧邻的重复目录复拉校验，保留API客户端在报价POST前的唯一校验及后端最终权威校验。
- 新增定向合同，锁定新鲜选择只经过一次目录复拉后发出报价请求，真实目录或商品失效仍在POST前失败关闭。
- 同步更新技术约束、问题台账和V1验收矩阵；不涉及支付、WINLA、后端、数据库或部署。

## 2026-08-21 — OPEN-12-DEV-038-01

- 将当前提交与已确认 CloudBase DEV 版本更新为 `2ff264c` / `huaren-api-dev-038`，登记精确实例与发布链。
- 依据固定 038 实例首次启动日志和业务探针证据关闭 OPEN-12。
- 将一次未影响探针的 Hikari `connection closed` 告警归入 OPEN-02／P1 继续观察，不触发重启或配置修改。

## 2026-08-21 — OPEN-12-FLYWAY-SCHEMA-01

- DEV 037 确认数据库连接成功，但自定义 Flyway 在执行迁移 SQL 前因 `currentSchema=null` 启动失败；DEV 已回退 036。
- `releaseFlyway` 改为复用已冻结的 `hz.environment.database-name` 作为 `defaultSchema`，不新增平台环境变量、不 repair、不改迁移 SQL。

## 2026-08-21 — OPEN-11-QUOTE-CATALOG-02

- DEV复测确认首轮修复仍会跨多个有效运营商支持集连接商品；报价选择现与公开目录完全对齐为“最高有效支持集＋其中最高有效目录”。
- 新增双有效支持集回归测试；报价服务定向测试8/8通过，真实DEV验证待本次发布后执行。
- 不改变目录内容、价格、支付、充值提交或数据库结构。

## 2026-08-21 — OPEN-11-QUOTE-CATALOG-01

- P012 报价创建统一绑定同一运营商支持集下版本号最大的当前有效目录，避免历史目录仍有效时把单个商品误判为多条可信商品。
- 新增双有效目录回归测试；报价服务定向测试7/7通过，真实DEV验证待发布后执行。
- 同步更新技术约束、问题经验和V1验收矩阵；不涉及支付、充值提交、真实资金或数据库结构变更。

## 2026-08-21 — P010-PHONE-LOCAL-01

- P010 手机号输入改为孟加拉用户熟悉的 `01xxxxxxxxx` 11位本地格式，不再拆分显示固定 `+880`。
- 同步更新产品需求、UI/高保真交互口径、需求—实现映射和前端实现；服务端继续统一号码规范化。
- 本次不改变运营商映射、商品目录、报价、订单、支付或充值提交规则。

## 2026-08-21 — OPEN-01 CLOSED

- 固定DEV实例真实微信登录全链通过：会话创建201、会话确认200且为`BUYER_SESSION_READY`，页面进入已登录状态。
- `FN-MP-02`更新为`REAL_PASS`，项目转入TRIAL-02无资金DEV业务旅程。
- 登记`SEC-DEV-01`：验证截图再次展开临时令牌；仓库与台账不保存令牌，后续证据必须折叠敏感响应。

## 2026-08-21 — OPEN-01/OPEN-10

- 固定DEV实例真实微信会话创建返回201，确认SafeLink、微信身份和TLS首因已闭合。
- 发现登录后会话确认误请求不存在的 `/buyer-api/v1/session`；新增OPEN-10并改为真实存在的 `/buyer-auth/v1/session`，验证稳定业务字段后进入已登录状态。
- 登记DEV证据截图误展开会话令牌事件；文档不保存令牌，后续截图禁止展开敏感响应。

## 2026-08-21 — PLAN-MAP-01

- 新增 `14-V1需求UI实现映射.md`，逐项映射16个需求业务页面、2个法律路由、42个SVG、后台工作区和28项验收矩阵。
- 收敛统计口径：18个正式路由=16个业务页+2个法律页；后台21个正常态SVG不含状态组件板和早期A130综合设计源。
- `TRIAL-01`更新为来源和映射已冻结；视觉一致性及逐状态真实数据证据仍待执行。

## 2026-08-21 — PLAN-V1-SOURCE-01

- 将 `项目管理/V1需求规格/` 确认为V1需求与UI的首要历史来源，并在来源映射中明确各文件职责。
- 新增 `13-V1上线试运行执行规划.md`，按全功能跑通、真实外部证据、候选晋级和小范围生产试运行重排11个工作包。
- 明确原始需求、后续批准决策、当前实现和运行证据的裁决顺序，禁止用现有代码反向删除需求。
- 明确完整门禁按批次候选执行，小问题只做定向验证，避免每次小改触发重型发布。

## 2026-08-21 — TRIAL-01

- 新增 `12-V1全功能验收矩阵.md`，以28个唯一功能 ID 覆盖小程序、运营后台及跨域上线能力。
- 以代码中的18个小程序页面、8个后台工作区和正式 API 入口校正验收范围。
- 建立时不追认未绑定固定版本的历史结果；DEV_PASS/REAL_PASS 计数从0开始，避免静态或本地证据冒充真实跑通。
- 当前唯一首个阻断项仍为 `FN-MP-02 / OPEN-01`。
- 校正小程序口径为18个已登记正式页面；`pages/support/case.vue` 为未登记、无正式导航且后端入口缺失的非执行残留，登记为 `GAP-UI-01`，不擅自接入V1。

## 2026-08-21 — PLAN-TRIAL-001

- 当前交付目标调整为“全部 V1 功能跑通并启动上线试运行”。
- 任务顺序改为：OPEN-01 清零→DEV 全功能→真实小额交易→TEST/STAGE 同提交晋级→试运行准备→PROD 小范围试运行。
- 增加 TRIAL-01 至 TRIAL-06 唯一任务编号、责任角色、状态、证据路径、下一动作和完成条件。
- 明确阶段内定向修复、阶段末集中候选发布，避免每个小问题触发重型发布。
- 真实资金和 PROD 发布仍须南哥对冻结版本、金额、环境和 RunId 分别授权。

## 2026-08-21 — BASELINE-001

- 建立 `docs/baseline/` 唯一项目基线目录。
- 归拢项目总览、治理权限、角色通讯、产品需求、UI、技术架构、开发约束、质量证据、部署运维、问题经验和版本计划。
- 根目录增加 `AGENTS.md`，强制所有角色与任务从本基线读取当前要求。
- 增加 `tools/check-project-baseline.mjs` 轻量结构核验器，不触发构建或部署。
- 旧文档统一降级为来源、历史、批准记录或证据，不删除、不覆盖。
- 登记接任前现有未提交文档，禁止误清理或误提交。
