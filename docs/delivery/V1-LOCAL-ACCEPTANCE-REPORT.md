# V1 本地候选验收报告

日期：2026-08-18
候选分支：`codex/v1-delivery-recovery`
起始提交：`335bffb`
结论：`LOCAL AUTOMATION GO / VISUAL EVIDENCE PENDING / REAL INTEGRATION NO-GO`

## 1. 本地已完成

- 后台已接正式持久化读写边界：A100客服案件创建与追加跟进、A110交易差异处置、A120审核、A121黄页、A122节假日与资讯、A130目录/渠道/价格/试算、A140正式订单/支付/充值/退款只读详情。
- A100/A110工作流使用角色权限、`expectedVersion`、`Idempotency-Key`、事件历史和失败关闭；不修改订单、支付、充值或退款终态。
- A140已删除Sandbox回退，仅允许FIN/CS/SUPER_ADMIN；号码仅返回掩码；权益仅返回用户可见白名单字段，不返回供应商编码、SKU或原始JSON。
- 小程序16个既定路由及微信客服直达已接正式DTO；外部支付或充值未配置时明确不可用，不伪造成功、不重复提交。
- V15-V21候选覆盖后台工作流、支付充值协调、不可变报价/订单快照、事务Outbox、UNKNOWN恢复任务、跨实例限额和供应商原始金额契约。

## 2. 自动化证据

- 后端完整测试：861 tests，0 failures，0 errors，66 skipped。
- 四环境Spring smoke：DEV/TEST/STAGE/PROD 4/4通过。
- 后台：contracts、state contracts、TypeScript类型检查、Vite build通过。
- 小程序：development、legacy、formal contracts、mp-weixin build、AppService load通过。
- 正式产物扫描：Mock、synthetic、sandbox、工程实现词命中0。
- `git diff --check`：通过。

以上只证明当前未提交本地候选的代码与自动化结果，不证明真实数据库、微信、供应商或云环境。

## 3. 尚缺的证据

- 21张后台高保真与16张小程序页面尚未在真实可运行状态逐帧截图并与Penpot做像素验收；源码和构建通过不能替代视觉证明。
- V15-V21尚未在真实MySQL 5.7执行空库V1→V21、现存V14→V21、重复执行、中断恢复和双实例并发验证。
- 最新候选尚未形成固定提交，也未产出经完整门禁绑定的最新Spring Boot可运行制品。
- 微信登录、微信支付、退款、WINLA充值、回调、查单及ECB汇率均未真实外连。

## 4. 本地P1登记

- 充值履约号码目前仍有明文持久化路径；真实用户进入前需采用受控密文、密钥版本和最短保留期。
- 登录限流仍主要绑定客户端请求引用；真实微信启用前需增加可信代理来源、设备或全局预算维度。
- A121既有城市缺少安全更新所需版本字段，因此保持只读，不允许盲目覆盖。

## 5. 需要用户统一支撑

1. 每个环境独立的`HZ_PHONE_DIGEST_HMAC_SECRET`（至少32字符）以及号码加密/KMS与保留期方案。
2. 微信小程序AppID/AppSecret、真实code2Session配置和一次真机登录。
3. 微信支付商户号、API v3 Key、商户私钥/证书、平台证书或公钥模式、回调域名，以及一次小额支付和退款授权。
4. WINLA正式协议、uid/Key、签名串、金额币种与scale、providerRef/requestRef稳定性、回调/查单/终态规则、余额、IP白名单、数据条款和一次小额充值授权。
5. CloudBase可信代理头规则与登录限流阈值。
6. ECB更新频率、有效期，以及汇率缓冲、加价、支付费、税费、最低毛利、舍入和促销承担正式值。
7. 正式角色账号、城市/黄页/节假日/资讯首批内容、域名、隐私协议及备案展示值。
8. 允许生成固定提交后，再批准真实MySQL门禁、真实截图验收及后续DEV部署。

## 6. 禁止推导

- `LOCAL GO`不等于可上线。
- H2迁移与并发测试不等于MySQL 5.7实证。
- Fake适配器测试不等于微信或WINLA真实协议通过。
- 构建成功不等于高保真像素验收通过。
- 本报告不授权提交、推送、数据库变更、外部调用或云部署。
