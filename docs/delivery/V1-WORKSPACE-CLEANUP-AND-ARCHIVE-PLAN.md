# V1 工作空间清理与归档计划

## 固定对象

- 交付候选工作树：`E:\workspace\huarenzaimeng\.worktrees\data-v11-v12-oracle`
- 候选分支：`codex/v1-delivery-recovery`
- 冻结起点：`335bffbba8769daa63cd67aa74e1f2ba2e8f446e`
- 变更清单：`docs/delivery/V1-CANDIDATE-CHANGE-MANIFEST.txt`
- 禁止纳入：`.gate-temp/`、`.codex-tmp/`、`target/`、`dist/`、`node_modules/`、本地日志、真实密钥。

## 分类规则

1. 交付源码与测试：纳入逻辑提交，必须有对应合同或行为门禁。
2. 需求、技术决策、高保真和环境模板：归档到 `docs/`，敏感值只使用占位符。
3. 历史证据：仅保留最终结论与必要清单；运行缓存、重复日志和临时产物不纳入 Git。
4. 无法确定归属：先列入待确认清单，不直接删除。

## 清理边界

- 可以删除：明确可再生成的编译输出、浏览器缓存、JUnit/Mockito 临时文件、已被最终报告替代的临时日志。
- 不能直接删除：未提交源码、数据库文件、迁移包、凭据、用户文档、唯一证据。
- 任何递归删除前必须再次解析绝对路径并确认仍位于工作空间内。

## 目标状态

- 候选分支仅保留源码、测试、迁移、文档和环境模板。
- `git status` 不再被缓存、日志或临时目录污染。
- 每个逻辑提交都可独立说明范围和门禁，不混入部署、数据库或外部调用。
