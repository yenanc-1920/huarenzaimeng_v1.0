# 华人在孟项目统一入口

所有项目角色、Codex 会话和自动化任务在开始任何项目工作前，必须先完整阅读：

1. `docs/baseline/README.md`
2. `docs/baseline/00-项目总览与当前状态.md`
3. 与任务类型对应的基线文件

项目规则、角色职责、需求、UI、技术、开发、测试、部署、问题经验和后续计划，均以 `docs/baseline/` 为唯一当前权威来源。

仓库其他文档仅作为来源、批准记录、历史过程或证据附件；与 `docs/baseline/` 冲突时，不得直接执行，必须按基线变更流程处理。

禁止在其他目录新建同类型“最新版”“最终版”或平行基线。任何规则调整先修改 `docs/baseline/` 中唯一对应文件，并登记到 `docs/baseline/CHANGELOG.md`。

